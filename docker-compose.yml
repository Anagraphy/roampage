services:
  roampage:
    build: .
    container_name: roampage
    restart: unless-stopped
    ports:
      - "80:3000"
    # environment:
    #   ENCRYPTION_KEY: "none"  # or a 64-char hex key — see README
    volumes:
      - ./data:/data

networks:
  newt:
    external: true
